
const add             = (a, b) => a + b;
const substract       = (a, b) => a - b;
const multiplication  = (a, b) => a * b;
const division        = (a, b) => a / b;

module.exports = {
  add,
  substract,
  multiplication,
  division
};